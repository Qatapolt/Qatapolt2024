import { scale, ScaledSheet, verticalScale } from "react-native-size-matters";

export const styles=ScaledSheet.create({
    imgContainer:{width:scale(200),height:verticalScale(160),alignSelf:"center",}

})